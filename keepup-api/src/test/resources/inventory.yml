feat/example:
  copyPaths:
    - from: should-correctly-set-dest
      to: plugins/example
    - should-correctly-default-dest
    - should-template-inventory-with-env
    - from: should-copy-individual-files/copy-individual
      to: plugins/example

example-host:
  variables:
    testVar: "{{ TEST_VAR }}"

  include:
    - feat/example
    - should-copy-feature

blacklist-test-host:
  copyPaths:
    - should-blacklist-jar-files
